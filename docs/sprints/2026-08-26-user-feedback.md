# SPRINT 2026-08-26 — 사용자 피드백 반영

## 목표

유저가 화면을 보고 준 피드백 3건. 디자인이 딱딱하다 / 로그인이 풀린다 /
관계자 계정을 분리하고 사용자 목록을 보여 달라. 여기에 BACKLOG 의 DATA-001(출하월)을
얹어 스프린트를 열었다.

---

## 태스크별 결과

### AUTH-001: 로그인 상태가 화면 간 이동에서 풀린다
- **완료 기준 달성**: ✅
- **한 일**
  - `lib/auth.ts` 를 구독 스토어로, `lib/useSession.ts`(`useSyncExternalStore`) 추가.
  - 역할을 계정에 묶었다 — 농가 `000000`, 금융기관 `222222`.
  - `/bank/applicants` 신설, 심사 대시보드를 접수 건 선택 방식으로 재구성.
- **핵심 결정**
  - **Context 대신 구독 스토어.** 이미 여러 컴포넌트가 `currentSession()` 을 직접
    부르고 있어 시그니처를 유지하는 쪽이 변경면이 작았다.
  - **차주 목록의 신원만 예시, 숫자는 전부 엔진 계산.** 가짜 심사 결과를 지어내면
    화면 전체가 거짓이 된다. 무엇이 예시인지 화면에 명시했다.
- **재현 명령어**
  ```
  cd apps/web && npx vitest run tests/auth.test.ts
  ```

### UI-001: 너무 각져서 딱딱하다
- **완료 기준 달성**: ✅
- **한 일**: 반경 2px → 6~10px(표는 각지게 유지), hover·focus·active 전이,
  `prefers-reduced-motion` 전역 존중. 리포트(`.sheet`)는 인쇄물이라 제외.
- **핵심 결정**: 움직임은 **상태 변화를 설명하는 것만**. 스크롤 연동·장식용 애니메이션은
  넣지 않았다. 정부 포털의 신뢰감은 색계와 정보 구조가 만들지 각진 모서리가 만들지 않는다.
- **부수 결과**: 접근성 위반이 오히려 줄었다 (로그인 3건 → 1건).

### DATA-001: 출하월 채우기
- **완료 기준 달성**: ❌ blocked — TASKS.md 로 이월
- KAMIS 로는 불가능하다는 것을 측정으로 확인했다. 상세는 TASKS.md 참조.

---

## harness 결과

```
python scripts/harness.py all
→ api 200 + web 26 / deps_check 0 / feature_view 0 / Doc Lint PASS
python scripts/ui_check.py http://localhost:3000   → 3건 (스킵 링크 등 인정 예외)
```

---

## 발견된 사고 / 새 규칙

| 사고 | 추가된 규칙 | 위치 |
|------|------------|------|
| 로그인 후 메인으로 가면 다시 '로그인' 으로 보였다. `UtilBar` 가 루트 레이아웃에 있어 remount 되지 않는데 세션을 `useEffect(…,[])` 로 한 번만 읽었다. | 세션은 `lib/useSession.ts` 로 구독한다. | `apps/web/_GUIDE.md` |
| 위 버그를 고치다 **로그인돼 있어도 튕기게** 만들었다. 서버 렌더·하이드레이션 첫 프레임에서는 세션이 항상 null 이다. | 접근 통제는 `ready === true` 인 뒤에만 판단한다. | `apps/web/_GUIDE.md` |
| 화면에 "농가 222222" 라고 잘못 떴다. 아이디를 키로 쓴 객체에서 JS 가 정수형 키 `"222222"` 를 앞으로 당겼다 (`"000000"` 은 앞자리 0 때문에 뒤로). | 계정은 배열로 두고, 표기는 인덱스가 아니라 role 로 찾는다. | `lib/auth.ts` + 회귀 테스트 |
| 로그인한 사용자에게 메인이 계속 로그인을 권했다. | 세션이 있으면 업무 화면 링크를 보여준다. | — |
| **커밋이 비어 있는데 성공으로 보고했다.** 다른 세션의 `git stash` 가 `add` 와 `commit` 사이에 끼어들어 스테이징이 풀렸고, 훅은 `✅ harness all 통과` 를 찍었다. **검사가 다 통과했는데 검사할 게 없었다.** | 커밋 전 `git diff --cached --stat` 확인, 0개면 중단 (pre-commit 훅으로 집행). | `.git/hooks/pre-commit` |
| 클론한 사람이 `rag.ingest` 를 안 돌리면 제도 근거가 **조용히** "근거 없음" 이 됐다. | 원문이 있으면 색인을 첫 요청 때 자동 생성한다. | `rag/ingest.py` + 테스트 2건 |

---

## 미완성 / 후속 (→ TASKS.md · BACKLOG.md)

- **DATA-001** 출하월 — `data.go.kr 15012299`(산지공판장 경락, 거래량 포함) 활용신청 대기
- **DATA-003** 이월 시세가 GARCH 국면 판정을 왜곡할 수 있다 (현재 수치는 무영향)
- 데모 로그인을 실제 인증으로 교체 — 배포 전 필수

---

## Reviewer Findings

| 심각도 | 항목 | 처리 |
|--------|------|------|
| 높음 | 로그인 상태가 화면 이동에서 풀림 | 수정 + 테스트 3건 |
| 높음 | 수정 중 회귀 — 로그인해도 튕김 | 수정, 사고 이력 기록 |
| 중간 | 데모 계정 라벨 역전 | 수정 + 회귀 테스트 |
| 중간 | 빈 커밋을 성공으로 보고 | 훅으로 기계 집행 |
| 낮음 | 글자크기 ± 버튼 제거 (유저 요청) | 반영 |
