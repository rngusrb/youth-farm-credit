# SPRINT 2026-08-28 (2차) — 남은 피드백과 검사 구멍

## 목표

C 팀원 피드백 15건 중 13건 반영 상태에서 남은 둘(「지난 분석 대비 변화」·「추천 이유」)을
끝내고, 두 스프린트 연속 사고를 숨겨 온 **검사 구멍**을 막는다.

TOOL-001 을 먼저 둔 이유: 이번에 고칠 화면 대부분이 로그인 뒤에 있어서,
그대로 두면 또 검사 없이 나간다. 실제로 그 판단이 맞았다 — 이번 스프린트에서 잡은
접근성 위반 25건 중 **24건이 TOOL-001 덕분에 처음 보인 것**이다.

## 태스크별 결과

### TOOL-001: ui_check 가 로그인 뒤 화면도 검사한다
- **완료 기준 달성**: ✅ (5/5)
- **한 일**: `meta/project_state.yaml` 에 세션 3종(farmer · farmer_with_history · bank)과
  대상 18개를 선언. `python3 scripts/ui_check.py all --all-viewports` 로 한 번에 돈다.
- **증명 3건**
  - 세션 유무로 같은 화면 내용 총량이 갈린다: `/bank/capacity` 101→146,
    `/bank/design` 101→177. 전에는 둘 다 101(같은 게이트)이었다.
  - 예외(`.sr-only` 스킵 링크)가 진짜 위반을 삼키지 않는다 — 6×13 링크·11px 글자를
    주입해 잡히는 것 확인.
  - 실패는 exit 1(없는 포트·없는 역할), 정상 0.
- **드러난 위반 24건** — 홈 짧은 링크 3 · 용어사전 목차 칩 19 · 용어사전 관련어 링크 ·
  내 농가 정보 버튼. **117 → 0건.**
- **재현**: `python3 scripts/ui_check.py all --all-viewports`

### UX-014: 왜 이 자금인지
- **완료 기준 달성**: ✅ (4/4)
- **핵심 결정 — "추천" 이라고 부르지 않는다.** 이 서비스는 대출 알선·상품 추천을
  하지 않는다고 선언했다(CLAUDE.md). 어느 쪽이 유리하다는 판정도 안 한다(규칙 2).
  조건과 숫자를 나란히 놓는 데서 멈춘다.
- **실측**: 같은 2억 1,082만원이 후계농(20년 상환) 6년차 1,370만원·위기 10.0%,
  우수후계농(10년 상환) 2,300만원·위기 49.3%. 한도 초과도 표시.
- 비교값은 상품마다 `runDiagnose` 를 따로 돌려 받는다(167ms). 화면 산술 없음.
- **재현**: `npx vitest run --root apps/web apps/web/tests/why-this-loan.test.ts`

### UX-015: 지난 분석과 무엇이 달라졌나
- **완료 기준 달성**: ✅ (4/4)
- **핵심 결정 — 차이를 숫자로 만들지 않는다.** 두 값과 방향만:
  `2억 1,082만원 ↑ 2억 6,800만원`, `10.0% ↓ 8.1%`. 방향에 좋다/나쁘다를 붙이지
  않는다 — 권장 금액이 늘어난 것이 좋은 일인지는 상황에 따라 다르다.
- 기록이 하나뿐이면 비교를 그리지 않는다. 입력이 무엇이 달라졌는지도 함께.
- **재현**: `npx vitest run --root apps/web apps/web/tests/report-diff.test.ts`

## harness 결과
```
python3 scripts/harness.py all      → 전 폴더 통과, Doc Lint PASS
python3 scripts/ui_check.py all --all-viewports → 총 0건 (대상 18 × 뷰포트 3)
```

## 발견된 사고 / 새 규칙

| 사고 | 추가된 규칙 | 위치 |
|------|------------|------|
| 「내 리포트」가 모바일에서 **실제로 246px 옆으로 밀려** 제목이 화면 밖으로 나갔다. 표의 `min-w-[680px]` 이 `overflow-x-auto` 를 뚫고 문서를 밀었다 | 좁은 화면에서는 최소폭을 강제하지 않고 보조 열을 접는다 | `app/app/reports/page.tsx` |
| 그 위반이 **한 번도 검사된 적 없었다** — 기본 세션엔 저장 기록이 0건이라 빈 화면만 쟀다 | 상태가 있어야 나오는 화면은 **그 상태를 가진 세션**을 따로 둔다 | `meta/project_state.yaml` (`farmer_with_history`) |
| 스킵 링크 잡음 54건(대상 18 × 뷰포트 3)이 진짜 위반을 묻고 있었다 | 포커스 전까지 접힌 요소는 터치 대상이 아니다. **클래스 이름이 아니라 계산된 스타일**로 판정 | `scripts/ui_check.py` |
| 상품 추천은 이 프로젝트가 하지 않기로 한 것인데, 화면에 그런 말이 새기 쉽다 | "추천"·"유리" 류 금지 — 회귀 테스트로 고정 | `apps/web/_GUIDE.md` · `tests/why-this-loan.test.ts` |

## 미완성 / 후속 (→ BACKLOG.md)

- **말투 혼용 기계검사** — 규칙 6·9 의 정당한 예외가 많아 단순 grep 이 못 쓴다.
  여전히 사람이 본다.
- `ui_check` 자동 실행은 여전히 안 한다(서버 필요). 노후 감지만 정확해졌다.
- 데모 로그인은 실제 인증이 아니다. 인증이 붙으면 `ui_check.sessions` 는 폐기 대상.

## Reviewer Findings

| 태스크 | 심각도 | 항목 | 처리 |
|--------|--------|------|------|
| TOOL-001 | — | findings 없음 | — |
| UX-014 | — | findings 없음 | — |
| UX-015 | High | 모바일 가로 스크롤 246px | 표를 반응형으로 |
| UX-015 | Medium | 그 화면이 검사된 적 없음 | 기록 있는 세션 추가 |
