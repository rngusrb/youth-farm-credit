# BACKLOG — 대기 이슈 및 태스크

> **분류 기준**
> - 대기 태스크: 설계 고민 필요, 여러 파일 수정 필요
> - 이슈 테이블: 한 줄 수정급, 단순 개선
> - 완료 항목은 즉시 삭제 (여기는 무덤이 아니다)
>
> 스프린트 시작 시 상위 3개를 TASKS.md 로 올린다.

## 현재 상태 (2026-08-27 기준)

> ⚠️ 이 날짜가 CLAUDE.md 최신 스프린트보다 과거면 harness Doc Lint FAIL.
> sprint-close 마다 동기화 의무. (화석 표 재발 방지)

**정체성**: 농가·금융기관 **양면** 여신설계 서비스. 작목·면적·부채를 넣으면 KOSIS 실측
소득변동성(σ)으로 몬테카를로를 돌려 **버틸 수 있는 대출 한도**와 **몇 년차에 무너지는지**를
계산하고, 문서번호가 붙은 근거로 설명한다. 농가는 `app/`(현금흐름·여신·안전성), 금융기관은
`bank/`(여신설계·상환여력·스트레스)로 같은 엔진을 다른 각도에서 본다.
숫자는 전부 `apps/api/engine` 이 만들고 LLM 은 문장만 쓴다 — 이 분리가 이 프로젝트의 존재 이유다.

**지금 상태** (2026-08-27): 양면 웹 + 월별 현금흐름 · 스트레스 테스트 엔진 · RAG 코퍼스 3종.
값마다 기준 시점 표시(`as_of`), 근거 접기(`<Fold>`), 심사 화면 출처 구분(`<SourceTag>`) 반영.
(recall@5 8/8) · 작목 38종 σ 실측. 레이어 경계 배선 완료(`core: []` 유지, deps_check 위반 0).
상세 → `docs/sprints/2026-08-26-service-buildout.md`,
      `docs/sprints/2026-08-27-team-feedback-2.md`

**다음 방향**: 기능은 섰고 **데이터 정밀도**가 병목이다. 아래 대기 태스크 상위 3개가 그것.

---

## 대기 태스크

| ID | 항목 | 비고 |
|----|------|------|
| DATA-001 | **출하월 38작목 중 6개뿐** — 제공기관 API 장애로 막힘 | 아래 상세 참조. 장애 복구 전까지 진행 불가라 TASKS 에서 내렸다 |
| DATA-002 | 도매가 시계열 **19작목** (KAMIS 매핑 21개 중) | DATA-003 작업 중 재수집됨. 남은 2개는 KAMIS 응답 없음(부추·참외) |
| RISK-001 | 농신보 보증료 미반영 — 상환여력이 그만큼 과대평가 | 지침에 요율이 없어 지어내지 않음. `policy.json` `not_modelled` 에 기록됨 |

---

## 팀 피드백 (2026-08-27 접수, 3명)

> 원문은 `docs/feedback/2026-08-27-team.md`. 여기엔 태스크로 쪼갠 것만 둔다.
> **셋 다 공통으로 지적한 것이 셋** — 정보량 과다 · 말투 · "그래서 뭘 하라는 건가".
> 그 셋이 최우선이다.

### 공통 지적 (3명 중 2명 이상)

| ID | 항목 | 근거 · 판단 |
|----|------|------------|
| UX-001 | **정보량 과다 — 결론 먼저, 나머지는 접기** | 3명 전원. "두괄식", "결론→이유→상세", "한 번에 너무 많음". **삭제가 아니라 접기다** — 근거 투명성이 이 서비스의 차별점이라 없애면 안 된다. `<details>` 또는 '자세히 보기' |
| UX-002 | **말투를 부드럽게** | 3명 전원. 다만 **적용 범위를 나눈다**(아래 판단 참조) |
| UX-003 | **"그래서 뭘 해야 하나"가 약하다 — 다음 행동으로 연결** | 2명. 리포트의 '무엇을 할 수 있나'가 "면적 늘려라" 수준. 이미 있는 맞춤금융지원·구제제도·제도근거 화면으로 바로 넘기기 |

### 용어 · 표현

| ID | 항목 | 근거 · 판단 |
|----|------|------------|
| UX-004 | 어려운 용어에 **호버 주석** (σ, DSCR, 차입 원금 …) | 용어사전이 있어도 읽는 흐름이 끊긴다. `<abbr>`+툴팁, 용어사전 링크 |
| UX-005 | 용어를 **비유·예시**로 풀기 | "설명해도 어려움". 용어사전 항목마다 한 줄 예시 |
| UX-006 | 소제목을 질문형·친절체로 | 예시 제공됨: "왜 6년차인가"→"6년차부터는 왜 어려울까요?", "이 계산을 믿어도 되나"→"보고서에 사용된 근거" |
| UX-007 | 농가 화면에서 전문용어를 의미 위주로 | "DSCR"→"상환여력 안정적". **부분 반대**(아래) |

### 표현 · 시각화

| ID | 항목 | 근거 · 판단 |
|----|------|------------|
| UX-008 | 리포트 밖 화면에도 **표·그래프** | "숫자 나열이라 직관적이지 않음". 현재 차트는 리포트와 수익전망에만 있다 |
| UX-009 | **정보 기준 시점 표시** (`2026.08 기준`) | 싸고 신뢰도를 크게 올린다. 데이터마다 조사연도가 이미 있으니 노출만 하면 된다 |
| UX-010 | 값의 출처 구분을 금융기관 화면에도 | 농가 입력 / 공공데이터 / 추정값. `AssumedBadge` 가 이미 있으니 확장 |
| UX-011 | 디자인 밀도 — '집지켜' 참고 | **확인 필요**: 해당 서비스를 아직 보지 않았다. 무엇을 참고하라는 것인지 먼저 확인 |

### 기능

| ID | 항목 | 근거 · 판단 |
|----|------|------------|
| FEAT-001 | **거치 후반기(4~6년차) 농가 라우팅 강화** | "연체 직전 농가가 제일 도와야 할 대상인데 구제제도가 재해 위주". 원래 명세 §4.2 에 있던 항목이다. 우선순위 높음 |
| FEAT-002 | **시나리오 비교** — 5천/7천/1억, 가격 −10%/−20% 나란히 | 안전진단에 원금 조절이 있으나 **나란히 비교**가 없다 |
| FEAT-003 | 추천 이유 표시 — "청년농 + 면적 + 경영기간 조건 해당" | 지금은 상품을 나열만 한다 |
| FEAT-004 | 조건에 맞는 정보만 필터링 | "단순 나열보다 내 조건에 맞는 것만" |
| FEAT-005 | 지난 분석 대비 변화 (소득 전망 ↑, 상환부담 ↓) | **우선순위 낮게 봄**(아래) |
| FEAT-006 | ~~제외 이유 표시~~ | **반대**(아래). 대안으로 '확인 필요 요건' 체크리스트 |

### 내 판단 — 그대로 받지 않은 것

- **UX-002 말투** — 앱 화면은 `~해요` 찬성. **리포트는 반대.** 문서번호가 붙은 공식
  리포트에 해요체를 쓰면 오히려 신뢰가 깎인다. 그리고 이 서비스는 "이 돈은 못 갚을 수
  있습니다"를 전해야 하는데, 너무 부드러우면 경고가 경고로 안 읽힌다.
  → **앱 화면 해요체 / 리포트 합니다체 유지, 소제목만 질문형**으로 제안한다.

- **UX-007 용어 치환** — DSCR 을 없애고 "상환여력 안정적"만 남기는 데는 반대.
  용어를 지우면 농가가 **은행 가서 같은 말을 못 쓴다.** 이 서비스의 목적 중 하나가
  협상 준비다. → 평이한 말을 앞에, 용어를 괄호로 병기.

- **FEAT-006 제외 이유** — 반대. 자격 요건(연령·영농경력·교육이수)을 우리가 판정해
  "당신은 해당 없음" 이라고 하면, **틀린 판정 때문에 자격이 있는 사람이 신청을 포기**할
  수 있다. 지침 원문은 있지만 유권해석은 우리 몫이 아니다.
  → 단정하지 않고 **'확인 필요 요건' 체크리스트 + 원문 인용**으로 바꿔 제안한다.

- **FEAT-005 지난 분석 대비 변화** — 낮게 본다. 기록이 브라우저에만 있어 기기를 바꾸면
  끊기고, 데모에서 "변화"를 보여줄 이력을 만들기도 어렵다. 서버 저장이 생기면 그때.

- **UX-001 정보량** — 방향은 동의하나 **지우지는 않는다.** 근거를 다 보여주는 것이
  이 서비스가 신뢰를 얻은 이유이기도 하다(3명 다 "근거 자료가 설득력 있다"고 했다).
  접어서 숨기되 한 번에 펼 수 있게.

### DATA-001 상세 — 왜 막혔나

KAMIS 관측일수로 출하월을 역산하려 했으나 실패했다. 21작목 중 17개가 관측일수
983/984일에 최저·최대 비율 0.86 으로 동일했는데, 그 0.86 은 계절성이 아니라
**달의 영업일수 차이**(2월 20일 vs 3월 23일)였다. KAMIS 는 제철이 아니어도 매일
시세를 낸다 — 참깨는 24,407원이 5일 연속 같았다. 검증용 4작목 중 딸기 2종만 재현.

거래량이 있는 **산지공판장 경락 API**(data.go.kr 15012299)로 갈아타려 했고
활용신청은 반영됐다(`SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 소멸). 그런데 필수
파라미터를 명세대로 다 넣어도 `HTTP_ERROR / returnReasonCode 04` 만 돌아온다.

**우리 요청 문제가 아니라는 근거** — 없는 경로는 `NO_OPENAPI_SERVICE_ERROR`,
우리 경로만 `HTTP_ERROR`. 게이트웨이는 인식하고 백엔드가 응답하지 않는다.
명세의 샘플 날짜(20030124)조차 실패하고 http/https·JSON/XML 모두 동일.

```
curl "https://apis.data.go.kr/B552895/openapi/service/OrgPriceJointMarketService/getJointMarketPriceList\
?serviceKey=$DATA_GO_KR_SERVICE_KEY&numOfRows=3&pageNo=1&delngDe=20030124&jmrktCd=661101"
```

**재개 조건**: data.go.kr 장애 신고 → 제공기관(농림수산식품교육문화정보원) 복구.
대안(서울 열린데이터광장 가락시장 반입량)은 별도 키가 필요하고 가락시장 기준이라
산지와 다를 수 있어 보류.

---

## 이슈 테이블

| 심각도 | 항목 | 발견 |
|--------|------|------|
| 🟠 High | **`ui_check` 가 로그인 뒤 화면을 검사하지 못한다.** 세션·차주 선택 게이트에 막혀 `/bank`, `/bank/capacity`, `/bank/design` 이 전부 로그인 화면(내용 101단위 동일)만 측정되고 있었다. 그 뒤에서 모바일 업무 탭이 38px 로 WCAG 2.5.5 위반 중이었고 아무도 몰랐다(이번에 `min-h-11` 로 수정). 지금은 별도 스크립트로 `localStorage` 를 심어야 잰다 — **하네스에 안 들어간 검사는 결국 안 도는 검사다.** `ui_check.py` 에 `--session` 옵션을 넣고 `meta/project_state.yaml` 의 `ui_check.watch` 에 심사 화면을 등록할 것 | 2026-08-27 |
| 🟡 Medium | RAG 상위 1건이 틀리는 질문 존재 (`지원 자격 나이 제한` 4위) | 2026-08-26 |
| 🟢 Low | dev 서버가 새 라우트를 404 — 라우트 추가 후 `.next` 삭제 필요 (운영 메모) | 2026-08-26 |
| 🟢 Low | **AI 상담 "This page couldn't load" — 재현 안 됨.** 푸시된 커밋 중 해당 불일치가 있었던 이력이 없고(전수 확인), 깨끗한 클론 프로덕션 빌드에서 `/app/assistant` 정상 프리렌더. 원인 추정: 편집 중이던 dev 서버의 일시 상태 또는 stale `.next`. 재발하면 `rm -rf apps/web/.next` 후 재시작 | 2026-08-27 |

---

## 보류 안건 (유저 결정 대기)

| 안건 | 재상정 조건 |
|------|------------|
| (없음) | |
