# BACKLOG — 대기 이슈 및 태스크

> **분류 기준**
> - 대기 태스크: 설계 고민 필요, 여러 파일 수정 필요
> - 이슈 테이블: 한 줄 수정급, 단순 개선
> - 완료 항목은 즉시 삭제 (여기는 무덤이 아니다)
>
> 스프린트 시작 시 상위 3개를 TASKS.md 로 올린다.

## 현재 상태 (2026-08-26 기준)

> ⚠️ 이 날짜가 CLAUDE.md 최신 스프린트보다 과거면 harness Doc Lint FAIL.
> sprint-close 마다 동기화 의무. (화석 표 재발 방지)

**정체성**: 농가·금융기관 **양면** 여신설계 서비스. 작목·면적·부채를 넣으면 KOSIS 실측
소득변동성(σ)으로 몬테카를로를 돌려 **버틸 수 있는 대출 한도**와 **몇 년차에 무너지는지**를
계산하고, 문서번호가 붙은 근거로 설명한다. 농가는 `app/`(현금흐름·여신·안전성), 금융기관은
`bank/`(여신설계·상환여력·스트레스)로 같은 엔진을 다른 각도에서 본다.
숫자는 전부 `apps/api/engine` 이 만들고 LLM 은 문장만 쓴다 — 이 분리가 이 프로젝트의 존재 이유다.

**지금 상태** (2026-08-26): 양면 웹 + 월별 현금흐름 · 스트레스 테스트 엔진 · RAG 코퍼스 3종
(recall@5 8/8) · 작목 38종 σ 실측. 레이어 경계 배선 완료(`core: []` 유지, deps_check 위반 0).
상세 → `docs/sprints/2026-08-26-service-buildout.md`

**다음 방향**: 기능은 섰고 **데이터 정밀도**가 병목이다. 아래 대기 태스크 상위 3개가 그것.

---

## 대기 태스크

> DATA-001(출하월) 은 2026-08-26 TASKS.md 로 승격 — 여기서 뺐다.
> 승격한 항목을 양쪽에 두면 두 곳이 갈라진다.

| ID | 항목 | 비고 |
|----|------|------|
| DATA-002 | 도매가 시계열 **19작목** (KAMIS 매핑 21개 중) | DATA-003 작업 중 재수집됨. 남은 2개는 KAMIS 응답 없음(부추·참외) |
| RISK-001 | 농신보 보증료 미반영 — 상환여력이 그만큼 과대평가 | 지침에 요율이 없어 지어내지 않음. `policy.json` `not_modelled` 에 기록됨 |

---

## 이슈 테이블

| 심각도 | 항목 | 발견 |
|--------|------|------|
| 🟡 Medium | RAG 상위 1건이 틀리는 질문 존재 (`지원 자격 나이 제한` 4위) | 2026-08-26 |
| 🟢 Low | dev 서버가 새 라우트를 404 — 라우트 추가 후 `.next` 삭제 필요 (운영 메모) | 2026-08-26 |

---

## 보류 안건 (유저 결정 대기)

| 안건 | 재상정 조건 |
|------|------------|
| (없음) | |
