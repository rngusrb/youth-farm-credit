# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 남은 피드백과 검사 구멍 (2026-08-28)

C 팀원 피드백 15건 중 13건 반영. 남은 둘(「지난 분석 대비 변화」·「추천 이유」)을
끝내고, 두 스프린트 연속 사고를 숨겨 온 **검사 구멍**을 막는다.

TOOL-001 을 먼저 두는 이유: 이번에 고칠 화면 대부분이 로그인 뒤에 있어서,
지금 상태로는 **또 검사 없이 나간다.**

> 근거: `docs/sprints/2026-08-28-readable-for-farmers.md` 「미완성 / 후속」

---

### TOOL-001: ui_check 가 로그인 뒤 화면도 검사한다

**상태**: done
**우선순위**: high
**관련 파일**: `scripts/ui_check.py`, `meta/project_state.yaml`

### 설계 구상

`ui_check` 는 세션을 심지 못해 로그인 뒤 화면을 **한 번도 검사한 적이 없다.**
`/bank`, `/bank/capacity`, `/bank/design`, `/app/*` 이 전부 로그인 게이트만 재고
있었다 — 세 화면이 똑같이 '내용 101단위' 로 나온 것이 증거였다.

그 뒤에 실제로 위반이 쌓여 있었다 (실측):
- 모바일 업무 탭 35×44 (WCAG 2.5.5)
- 농가 홈 섹션 링크 3개 66×18
- 맞춤 금융지원 링크 118×18

지금은 임시 스크립트로 `localStorage` 를 심어 재고 있다. **하네스에 안 들어간
검사는 결국 안 도는 검사다** — 이번에 두 번 증명됐다.

세션·프로필을 `meta/project_state.yaml` 에 선언하고 대상 목록을 한 번에 돌린다.
계정은 이미 코드에 있는 데모 계정이라 새로 노출되는 비밀이 없다.

### 완료 기준

- [x] `ui_check` 가 세션을 심고 검사한다 (설정은 `meta/project_state.yaml`)
- [x] 대상 목록을 설정에 두고 한 번에 검사 (18개)
- [x] 로그인 뒤 화면이 실제로 측정되는지 **증명** — 게이트 화면과 내용 총량이 달라야 한다
- [x] 서버가 없거나 세션이 안 먹으면 **명확히 실패**한다 (0건 통과 금지)
- [x] harness all 통과

### 제약사항

- **데모 계정 외의 자격증명을 설정에 넣지 않는다.** 실제 인증이 붙으면 이 방식은 폐기
- `ui_check` 자동 실행은 여전히 안 한다 (서버 필요). 노후 감지만 정확해진다

### 구현 세부사항

`meta/project_state.yaml` 에 `ui_check.sessions`(farmer·bank, 데모 계정) 와
`ui_check.targets`(18개, 그중 12개가 로그인 뒤) 선언.
`python3 scripts/ui_check.py all --all-viewports` 로 한 번에 돈다.

**증명 3건**
- 세션 유무로 같은 화면 내용 총량이 갈린다: `/bank/capacity` 101 → 146,
  `/bank/design` 101 → 177. 전에는 둘 다 101(같은 게이트)이었다.
- 예외가 진짜 위반을 삼키지 않는다: 6×13 링크와 11px 글자를 주입해 잡히는 것 확인.
- 실패는 exit 1 (없는 포트·없는 역할), 정상 0.

**전수 검사로 드러난 새 위반 24건** (전부 그동안 한 번도 안 재던 자리)
홈 짧은 링크 3 · 용어사전 목차 칩 19 · 내 농가 정보 버튼 1 · 용어사전 관련어 링크.
117 → **0건**. 화면 18개가 전부 깨끗한 건 이 저장소에서 처음이다.

> 스킵 링크(`.sr-only`)는 포커스 전까지 1×1 이라 터치 대상이 아니다 — 검사에서 뺐다.
> 클래스 이름이 아니라 **계산된 스타일**로 판정하므로 진짜 작은 링크는 그대로 잡힌다.
> 이 잡음 54건(대상 18 × 뷰포트 3)이 진짜 위반을 묻고 있었다.

### Reviewer

**PASS** — findings 없음. 완료 기준 5/5, 증명 3건 모두 실측.

---

### UX-014: 왜 이 자금인지 — 고른 근거를 보여준다

**상태**: pending
**우선순위**: high
**관련 파일**: `apps/web/app/app/finance/`, `apps/web/components/`

### 설계 구상

팀원 지적: "그냥 정책자금 추천보다는 `청년농 + 재배면적 + 경영기간 조건에 해당해서
추천` 이런 식".

지금 「이용 가능한 정책자금」은 한도·금리·상환기간만 나열한다. **왜 이게 지금
조건에 맞는지**가 없다.

**"추천" 이라고 부르지 않는다.** 화법 규칙 2 — 우리가 내린 결론 대신 조건을 밝힌다.
이 서비스는 대출 알선·상품 추천을 하지 않는다고 선언했다 (CLAUDE.md).
할 수 있는 것은 **고른 근거를 보여주는 것**이다:

| 보여준다 | 보여주지 않는다 |
|---|---|
| 제도 조건(한도·거치·상환)과 근거 조항 | "이 자금을 추천합니다" |
| 같은 금액일 때 다른 자금과 상환액이 어떻게 다른지 | 어느 쪽이 유리하다는 판정 |
| 자격은 UX-012 자가진단으로 연결 | 자격이 된다/안 된다 |

상품 간 비교 숫자는 **엔진이 낸다** — 같은 원금·같은 농가로 다른 상품을 계산한다.
화면에서 곱하거나 나누지 않는다.

### 완료 기준

- [ ] 선택한 자금에 「왜 이 조건인가」 — 제도 근거 + 내 조건에서의 결과
- [ ] 같은 금액일 때 다른 자금과의 차이 (연 상환액·위기확률), 값은 엔진 응답
- [ ] "추천"·"유리" 같은 판정 문구 없음 — 회귀 테스트로 고정
- [ ] `ui_check` 회귀 없음 · harness 통과

### 제약사항

- **상품 추천 금지.** 이 프로젝트가 하지 않기로 한 것이다 (CLAUDE.md)
- 비교 숫자를 화면에서 만들지 않는다
- 자격은 UX-012 의 경계를 그대로 따른다 (의견까지, 판정은 없음)

---

### UX-015: 지난 분석과 무엇이 달라졌나

**상태**: pending
**우선순위**: medium
**관련 파일**: `apps/web/app/app/reports/`, `apps/web/lib/profile.ts`

### 설계 구상

팀원 지적: "지난 분석 대비 어떻게 변했는지도 있으면 좋을 듯 → 소득 전망 ↑,
상환부담 ↓ 이런 식".

`SavedReport` 에 이미 `riskLimit`·`crisisProb`·`savedAt` 이 쌓이는데 「내 리포트」는
목록만 보여준다. 비교가 없다.

**차이를 숫자로 만들지 않는다.** 두 값을 나란히 놓고 방향만 표시한다 —
`2억 1,082만원 → 2억 4,300만원 ↑`. 화면에서 뺄셈을 시작하면 그 값의 출처가
어디인지 아무도 모르게 된다 (전역 금지사항).

### 완료 기준

- [ ] 「내 리포트」에서 바로 앞 기록과 나란히 — 권장 금액·위기확률
- [ ] 기록이 하나뿐이면 비교를 **표시하지 않는다** (없는 비교를 만들지 않는다)
- [ ] 입력이 무엇이 달라졌는지도 함께 (작목·면적 등)
- [ ] `ui_check` 회귀 없음 · harness 통과

### 제약사항

- **차액을 화면에서 계산하지 않는다.** 두 값과 방향만
- 저장 시점(`savedAt`)만 쓰고 `new Date()` 로 "오늘" 을 찍지 않는다 (UX-009)
- 비교가 없을 때 빈 칸·0 을 채우지 않는다
