# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 농가가 읽기 쉽게 (2026-08-28)

C 팀원 피드백 15건을 코드로 대조한 결과 **6건 반영 · 4건 부분 · 5건 미반영**.
반영된 6건은 전부 **신뢰성** 쪽(출처·기준 시점·근거)이다 — 팀원이 이미 좋다고 한 부분을
더 단단하게 만든 것이다. 정작 **고쳐달라고 한 쪽**(피드백 7개 중 앞 4개가 전부
"농가가 읽기 쉽게")은 말투만 바꾸고 용어·정보량·필터링은 그대로다.

이번 스프린트는 그 넷을 잡는다.

> 근거: `docs/feedback/2026-08-27-team.md` · 직전 스프린트 `docs/sprints/2026-08-27-team-feedback-2.md`

---

### UX-011: 농가 화면 — 쉬운 말이 앞, 용어는 뒤

**상태**: done
**우선순위**: high
**관련 파일**: `apps/web/app/app/**`, `apps/web/lib/nav.ts`, `apps/web/components/gov/`

### 설계 구상

팀원 지적: "DSCR, 스트레스 테스트 같은 용어는 농가 화면에서는 `상환여력 안정적`,
`가격 하락 시 주의`처럼 의미 위주로 바꿔도 좋을 듯".

**그대로 따르지 않는다.** 확정 화법 규칙 4 가 정면으로 충돌한다 —
*"쉬운 말이 앞, 용어는 뒤. **용어를 지우지는 않는다** — 농가가 은행에서 같은 말을
써야 한다."* 이 규칙은 유저가 시안을 대목별로 고른 결과에서 뽑은 것이라
팀원 의견 한 줄로 뒤집지 않는다.

대신 **순서를 바꾼다**: 의미를 앞에, 용어를 괄호로 뒤에.
농가가 화면에서 뜻을 이해하고, 은행 창구에서는 같은 단어를 쓸 수 있어야 한다.

현재 농가 화면에 맨숫자로 떠 있는 것 (실측):
- `app/page.tsx` — `소득 변동성 σ 0.215`, `영업레버리지 2.46배`
- `app/safety/page.tsx` — `DSCR 기준`, `소득 변동성 σ`, `영업레버리지`
- `app/finance/page.tsx` — `DSCR 1.25 기준`
- `lib/nav.ts` — 「금융 안전진단 — 스트레스 테스트」

### 완료 기준

- [x] 농가 화면(`app/app/**`)의 지표에 **의미 문장이 먼저**, 용어는 괄호 안
      (실제: `보통 해에 얼마쯤 · 4,318만원 ~ 7,489만원` / `10년 중 8년이 이 사이 (소득 변동성 σ 0.215)`)
- [x] 메뉴 설명문에서 용어 단독 노출 제거 — `스트레스 테스트` → `가격이 떨어지면 어떻게 되나`
- [x] 바뀐 문구가 화법 규칙 2·3 위반이 아닐 것 (판정 금지 · 지시 금지)
- [x] 용어사전(`/glossary`) 항목과 화면 용어가 어긋나지 않을 것
- [x] `ui_check` 회귀 없음 · `harness apps/web` 통과

### 제약사항

- **용어를 지우지 않는다.** 규칙 4. 괄호 안에 남긴다
- **숫자를 화면에서 만들지 않는다.** `±21%` 같은 환산도 엔진이 낸 값이어야 한다
- 금융기관 화면(`app/bank/**`)은 **건드리지 않는다** — 실무 용어가 맞다 (규칙 6)

### 구현 세부사항

**엔진에 `income.band_p10_p90` 추가** (`engine/income.py`)
σ 를 화면에서 ±% 로 환산하면 화면이 만든 숫자가 된다. 엔진이 낸다.
`simulate.draw_paths` 와 **같은 분포**(`exp(σ·Z − σ²/2)`)를 쓰고, 40만 회 추출
분위수와 대조해 오차 0.005%·0.035% 를 확인했다 (`tests/test_income_band.py`).

**화면 문구** — 뜻이 앞, 용어는 괄호·보조줄로 뒤 (규칙 4, 용어 삭제 없음)
| 전 | 후 |
|---|---|
| `소득 변동성 σ 0.215` | `보통 해에 얼마쯤 · 4,318만원 ~ 7,489만원` / 보조줄 `10년 중 8년이 이 사이 (소득 변동성 σ 0.215)` |
| `영업레버리지 2.46배` | `수입이 줄면 소득은 몇 배로 줄까 · 2.46배` / 보조줄 `영업레버리지 2.46` |
| `DSCR 1.25 기준` | `버는 돈이 갚을 돈의 1.25배 (DSCR 1.25)` |
| `AI 권장 차입` | `소득 변동까지 반영` — 규칙 2(판정 금지). 처음 쓴 "무리 없는 금액" 은 우리가 내린 결론이라 뺐다 |
| 메뉴 `스트레스 테스트` | `값이 떨어지면 어떻게 되나` |
| 안전진단 하드코딩 `가격이 20% 떨어지면` | 시나리오의 `detail` 문자열을 엔진에서 그대로 인용 |

**곁들여 잡은 것 — 조사 오류 5건**
"2억 8,918만원**예요**", "5억원**를** 다 빌리면", "…원**가** 부족해요".
`won()` 은 받침 O, `pct()` 는 받침 X 라 조사가 갈린다. 화법 규칙 6 이 경고해 둔
자리인데 사람이 못 봤다 → `tests/particles.test.ts` 로 고정.

**ui_check 로 새로 드러난 접근성 위반 5건** (전부 로그인 게이트 뒤라 미검사 상태였다)
모바일 업무 탭 35×44(폭 부족) · 농가 홈 섹션 링크 3개 66×18 · 맞춤 금융지원 링크 118×18.
전부 `min-h-11`/`min-w-11` 로 수정.

### Reviewer

**1차 REVISE** — High 3건. 이 태스크의 목적이 화법인데 그 규칙을 새로 위반했다.
- 규칙 3 위반: "위 배수가 **그 뜻이에요**" (`…을 뜻합니다` 류 금지)
- 규칙 6 위반: 농가 영역(해요체)에 "빠**집니다**", "줄어**듭니다**" 혼입
- 완료 기준 "화법 규칙 2·3 위반 아닐 것" 을 검증 없이 [x] 로 표시

Medium 1건 — 같은 화면에서 같은 값이 `49%`(문단) vs `-49.2%`(StressTable) 로 갈렸다.
화면이 반올림 자릿수를 정하고 있었다 → `pct()` 로 통일.
Low 1건 — `import math` 가 함수 본문 안 → 모듈 상단.

**2차 PASS** — 전부 해소. 재감사 후 농가 영역에 남은 합니다체 4곳은 정당하다:
`page.tsx:122-123` 은 규칙 9(무차입으로도 생계 불가 — 단정형), `assistant:84` 는
서비스 입장 선언, `page.tsx:52` 는 오류 메시지.

> 이 부류(화법·조사)가 두 스프린트 연속 사람 눈을 통과했다. 조사는 테스트로 막았지만
> 말투는 규칙 6·9 의 정당한 예외가 많아 기계 검사를 만들지 않았다 — BACKLOG 로.

---

### UX-012: 내 조건 기준으로 걸러 보여주기

**상태**: done
**우선순위**: high
**관련 파일**: `apps/web/app/crops/`, `apps/web/app/library/`, `apps/web/app/app/relief/`, `apps/api/rag/`

### 설계 구상

팀원 지적: "정보가 단순 나열되는 것보다 내 농가 조건에 맞는 정보만 필터링해서
보여주는 느낌이 더 나면 좋겠음" + "반대로 왜 제외됐는지도 간단하게".

지금은 작목 데이터 38개 전체 나열, 자료실 문서 전체 나열이다. 저장된 농가 정보
(`yfc.profile.v1` — 작목·면적·부채)가 있는데 목록이 그걸 전혀 안 쓴다.

**제외 이유 — 판정이 아니라 의견으로 낸다.**
이전 스프린트에서 "자격 판정을 잘못 내리면 받을 수 있는 사람이 포기한다"는 이유로
통째로 뺐는데, 유저 의견대로 **의견 제시 수준**은 가능하다. 경계는 이렇게 둔다:

| 한다 | 하지 않는다 |
|---|---|
| "○○조 기준으로는 해당하지 않을 수 있어요" | "자격 없음" · "신청 불가" |
| 근거 조항 원문을 같이 보여준다 | 조항 없이 결론만 낸다 |
| "최종 판단은 시행기관" 을 붙인다 | 우리 판단을 최종처럼 보이게 한다 |
| 조항을 못 찾으면 **아무 말도 안 한다** | 없는 근거로 추측한다 |

마지막 줄이 핵심이다 — RAG 가 이미 쓰는 원칙(근거 없으면 답하지 않는다)과 같다.

**설계 수정 (2026-08-28)** — 처음엔 「구제제도 화면」에 붙이려 했는데 자리가 틀렸다.
구제제도의 해당 여부는 **피해율 30% 이상** 같은 미래 사건이라 지금 판정할 대상이
아니다. 팀원이 말한 "추천 이유 / 제외 이유" 는 **정책자금 자격**에 관한 것이고,
코퍼스에 조문 단위로 실측 확인된다:

| 요건 | 후계농 (Ⅱ-1) | 청년창업형 (Ⅱ-1) |
|---|---|---|
| 연령 | 18~49세 | 18~39세 |
| 영농경력 | 없거나 10년 미만 | 총 3년 이하 |
| 병역 | 미필자는 복무 완료 후 대출 | 병역필 또는 면제 |
| 거주지 | 신청 시·군에 실제 거주 | 〃 |
| 교육실적 | 농업계 졸업 또는 인정 교육 이수 | (해당 조항 없음) |

우리가 가진 농가 정보(작목·면적·부채)로는 이 중 **하나도 판정할 수 없다.**
그래서 **자가진단**으로 간다 — 농가가 답하면 그 답에 대해서만 의견을 낸다.
요건 목록과 조문은 하드코딩하지 않고 **코퍼스에서 원문을 끌어온다**.

### 완료 기준

- [x] 작목 데이터·자료실에 「내 조건만 보기」 — 저장된 농가 정보 기준
- [x] 농가 정보가 없으면 필터를 **숨긴다** (빈 필터를 두지 않는다)
- [x] 정책자금 **자격 자가진단** — 해당/비해당을 **의견 수준**으로 + 근거 조항 원문 병기
- [x] 조항을 못 찾은 요건은 **아예 표시하지 않는다** — 회귀 테스트로 고정
- [x] `harness apps/web`, `apps/api` 통과

### 제약사항

- **비해당을 단정하지 않는다.** 위 표의 왼쪽 칸만 쓴다
- 필터가 정보를 **지우지 않는다** — 끄면 전체가 다시 보여야 한다
- 근거 조항은 RAG 검색 결과를 그대로 인용한다 (요약 금지)

### 구현 세부사항

**`rag/eligibility.py`** — 요건의 **구조**(문서·조항번호·판정방식)만 `loan_products.json`
에 두고, 문장은 코퍼스 원문에서 끌어온다. 조문을 못 찾으면 그 요건이 응답에서 빠진다.
`GET /api/v1/eligibility`.

> 사고: 우수후계농에 후계농의 Ⅱ-1-가~마(연령·병역·교육)를 복사해 넣을 뻔했다.
> 실제 그 지침에는 그 조항이 **없고** 요건이 「후계농 선정 후 5년 이상」 하나다.
> 처음 검색이 doc_title 부분일치라 "우수후계농업경영인 육성사업"까지 같이 잡은 탓이다.
> 요건을 코드에 적기 시작하면 이렇게 샌다 → `test_eligibility.py::test_문서마다_요건이_다르다`

> 조항 번호만으로 특정이 안 되는 경우가 있다. 우수후계농 `Ⅱ-1` 은 서식·표 조각이
> 같은 번호를 달고 있어 길이로 고르면 "1    년차" 가 뽑혔다 → 데이터에 `contains` 추가.

**`EligibilityCheck.tsx`** — 판정하지 않는다. 낼 수 있는 말은 셋뿐:
`답하시면 의견을 붙여 드려요` / `Ⅱ-1-가 기준 범위 안이에요` /
`Ⅱ-1-가 기준으로는 해당하지 않을 수 있어요`. 조항 원문은 `<Fold>` 안에.
"자격 없음"·"신청 불가" 류가 못 새도록 테스트로 고정.

**필터** — 실측: 작목 38→15건(같은 분류), 자료실 3→1건. 끄면 전체 복원.
농가 정보가 없거나 분류·지침을 못 찾으면 버튼 자체를 그리지 않는다.

### Reviewer

**1차 REVISE** — High 1건.
- 화면이 **틀린 실패 원인**을 댔다. 네트워크 오류로 못 받아왔는데도 "자료실에서
  조항을 찾지 못해 표시하지 않아요" 라고 말했다. 조용히 넘기지는 않았지만
  틀린 이유를 대는 건 더 나쁘다 → 로딩중 / 불러오기 실패 / 조항 없음 세 상태로 분리.
- Medium: 조문 400자 절단이 영농경력 조항의 독립경영 요건(직계존속 임차·공유지분)을
  잘라내고 있었다. 요건 일부만 보고 대보면 틀린 결론이 난다 → 1200자, 잘림 0건.
- Low: 나이·경력 입력이 상품 간 공유. 기준이 갈리는 순간 깨지는 테스트로 예약.

**2차 PASS** — 전부 해소. deps_check 통과(core 는 자격을 판정하지 않는다).

---

### UX-013: 농가 화면에 금액별 비교

**상태**: pending
**우선순위**: medium
**관련 파일**: `apps/web/app/app/finance/`, `apps/web/components/`

### 설계 구상

팀원 지적: "대출 5천/7천/1억일 때 어떻게 달라지는지".

가격 하락 시나리오는 농가 안전진단에 이미 있다. **금액별 비교는 금융기관 화면에만**
있는데(`app/bank/design`), 정작 "얼마를 빌릴까"는 농가의 질문이다.

엔진은 이미 `scenarios` 로 금액별 결과를 낸다 — 화면만 붙이면 된다.
새로 계산하지 않는다.

### 완료 기준

- [ ] 「맞춤 금융지원」에 금액 3~4개를 나란히 — 상환액·위기확률·절벽 연차
- [ ] 권장 금액이 어느 칸인지 눈에 띌 것
- [ ] 값은 전부 엔진 응답 그대로 (화면 계산 금지)
- [ ] `ui_check` 회귀 없음 · `harness apps/web` 통과

### 제약사항

- **금액을 화면에서 만들지 않는다.** 비교할 금액도 엔진이 정한다
- 표가 넓어지면 `overflow-x: auto` — 페이지 가로 스크롤 금지
- 판정 문구 금지 (규칙 2) — "이 금액이 안전합니다" 가 아니라 조건을 밝힌다
