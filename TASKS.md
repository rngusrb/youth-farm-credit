# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 데이터 출처 정리 (2026-08-26)

직전 스프린트(사용자 피드백 반영)의 AUTH-001·UI-001 은 완료·배포됨 →
`docs/sprints/2026-08-26-user-feedback.md`. DATA-001 은 여기로 이월했다.

---

### DATA-001: 출하월이 38작목 중 6개뿐
**상태**: blocked — 외부 API 활용신청 대기 (유저)
**우선순위**: high
**관련 파일**: `apps/api/stats/`, `apps/api/data/crops.json`

### 무엇을 시도했고 왜 막혔나
KAMIS 일별 도매가의 **월별 관측일수**로 출하월을 역산하려 했다. 실패했다.

- 21작목 중 17개가 관측일수 983/984일, 최저/최대 비율 0.86 으로 **완전히 동일**했다.
  0.86 은 계절성이 아니라 **달의 영업일수 차이**(2월 20일 vs 3월 23일)였다.
- KAMIS 는 제철이 아니어도 매일 시세를 낸다. 참깨는 24,407원이 5일 연속 같았다 —
  이월된 시세지 실제 거래가 아니다.
- 검증용 4작목 중 **딸기 2종만 재현**(6~10월 관측 0), 토마토·시금치는 실패.

억지로 임계값을 맞추면 17작목에 **틀린 출하월**이 들어간다. 멈추는 쪽을 택했다.

### 찾아본 대체 출처
| 후보 | 판정 |
|---|---|
| KAMIS 일별 도매가 | ❌ 응답에 물량 필드 없음 (가격만) |
| 농사로 작목별 재배력 | △ 작목별 PDF/HWPX — 파싱 비용 큼 |
| 농사로 OpenAPI | △ 재배 시기 제공, 별도 키 발급 필요 |
| **산지공판장 경락가격정보** (data.go.kr 15012299) | ◎ **거래량 포함**, 자동승인 |

### 활용신청 후 — 제공기관 서비스 장애로 다시 막힘 (2026-08-26)

신청은 반영됐다. `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 가 사라졌다.
명세 문서(`EPIS_NKOD_OP_OpenAPI활용가이드`, 2016년판)를 받아 필수 파라미터
`jmrktCd`(공판장코드) 까지 맞췄는데도 응답이 `HTTP_ERROR / returnReasonCode 04` 다.

**우리 요청 문제가 아니라는 근거**

| 호출 | 응답 |
|---|---|
| 존재하지 않는 경로 | `NO_OPENAPI_SERVICE_ERROR` |
| 우리 경로 | `HTTP_ERROR (04)` |

게이트웨이는 경로를 인식하고 백엔드로 넘기는데 백엔드가 응답하지 않는다.
날짜를 2003·2019·2022·2023·2025 로 바꿔도 전부 같고, 명세의 **샘플 날짜
(20030124)** 조차 실패한다. `http`/`https`, JSON/XML 모두 동일.

재현:
```
curl "https://apis.data.go.kr/B552895/openapi/service/OrgPriceJointMarketService/getJointMarketPriceList\
?serviceKey=$DATA_GO_KR_SERVICE_KEY&numOfRows=3&pageNo=1&delngDe=20030124&jmrktCd=661101"
```

### 재개 조건
data.go.kr 장애 신고 → 제공기관(농림수산식품교육문화정보원) 복구.
복구되면 거래량으로 출하월을 역산한다 — 거래량이 있으면 "실제로 물건이 나오는 달"이
직접 나온다.

대안(보류): 서울 열린데이터광장 가락시장 반입량. 별도 키가 필요하고 가락시장
기준이라 산지와 다를 수 있어, 장애 복구를 먼저 기다린다.

### 제약사항
- KAMIS 매핑 없는 17작목은 여전히 못 채운다 — '미상' 유지, 추정하지 않는다
- 임계값 근거를 결정문서에 남길 것

---

### DATA-003: 이월 시세가 시장 국면 판정을 왜곡할 수 있다
**상태**: done → `docs/decisions/0002-carried-quotes-suppress-regime.md`
**우선순위**: medium
**관련 파일**: `apps/api/estimators/garch.py`, `apps/api/stats/calibrate_market.py`

### 설계 구상
DATA-001 을 파다가 나왔다. KAMIS 는 거래가 없어도 직전 시세를 이월한다.
가격이 **실제로 움직인 날**의 비율을 재보니 작목마다 크게 갈렸다.

| 작목 | 변동일 비율 |
|---|---|
| 시금치 | 97% |
| 토마토 | 87% |
| 딸기 | 85% |
| 수박 | 73% |
| **참깨** | **20%** |

**먼저 확인한 것**: 지금 화면에 나가는 교차검증 σ(`annual_price_sigma`)는
연평균으로 집계한 뒤 연간 변화율을 보므로 이월 시세에 강건하다. 유효일수까지
보정해 재계산하면 1.343 → 1.344 로 사실상 변화가 없다. **현재 수치는 문제없다.**

문제는 앞으로다. GARCH 국면 판정은 **일별** 수익률을 쓰는데, 참깨처럼 80%가
이월이면 "평소보다 조용함"이 시장이 아니라 **집계 방식**을 반영하게 된다.
지금은 시장 데이터가 4작목뿐이고 전부 73% 이상이라 안 걸렸지만, DATA-002 로
작목을 늘리면 바로 문제가 된다.

### 완료 기준
- [ ] 변동일 비율을 재서 임계값 미만이면 국면 판정을 **내지 않는다** (거짓 국면 금지)
- [ ] 임계값과 근거(위 실측치)를 코드 주석과 결정문서에 남긴다
- [ ] 비율을 `crops.json` `market` 에 기록해 화면에서 확인 가능하게
- [ ] harness apps/api/estimators, apps/api/stats 통과

### 제약사항
- **한도 계산에 영향을 주지 않는다.** 국면은 안내용이며 σ 는 KOSIS 실측을 쓴다
- 판정을 못 하면 '판정 보류' 로 표시한다 — 임의로 '평상'이라고 하지 않는다
