# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 사용자 피드백 반영 (2026-08-26 착수)

유저 피드백 3건 + BACKLOG DATA-001.

---

### AUTH-001: 로그인 상태가 화면 간 이동에서 풀린다
**상태**: done → `docs/sprints/` (스프린트 종료 시 아카이브)
**우선순위**: high
**관련 파일**: `apps/web/lib/auth.ts`, `apps/web/components/gov/UtilBar.tsx`

### 설계 구상
증상: 로그인 후 메인(`/`)으로 가면 상단이 다시 "로그인"으로 보인다.
원인은 저장소가 아니라 **상태 공유**다 — `UtilBar` 가 루트 레이아웃에 있어
클라이언트 이동 시 remount 되지 않는데, 세션을 `useEffect(…, [])` 로 한 번만 읽는다.
`signIn()` 은 localStorage 만 바꾸고 이미 떠 있는 구독자에게 알리지 않는다.

구독 가능한 작은 스토어로 바꾼다 (`subscribe`/`getSnapshot` + `useSyncExternalStore`).
대안으로 Context Provider 도 있었지만, 이미 여러 클라이언트 컴포넌트가 `currentSession()`
을 직접 부르고 있어 함수 시그니처를 유지하는 쪽이 변경면이 작다.

같은 태스크에서 **관계자 계정을 분리**한다. 지금은 아이디 하나로 라디오 버튼을 골라
역할이 정해지는데, 그러면 "아무나 금융기관 화면에 들어간다". 계정이 역할을 정하게 한다.
- 농가 `000000` / `111111`
- 금융기관 `222222` / `333333`

### 완료 기준
- [ ] 로그인 후 어느 화면으로 이동해도 상단에 계정·로그아웃이 유지된다
- [ ] 두 탭을 띄워 한쪽에서 로그아웃하면 다른 쪽도 반영된다 (storage 이벤트)
- [ ] 농가 계정으로 `/bank` 접근 시 차단, 반대도 동일
- [ ] `apps/web/tests/auth.test.ts` 확장 — 계정별 역할, 구독 알림
- [ ] harness apps/web 통과

### 제약사항
- 데모 인증이라는 사실을 화면과 코드 주석에서 계속 명시할 것 (실제 인증 아님)
- 서버로 개인정보를 보내지 않는다 (`apps/web/_GUIDE.md` 금지사항)

---

### UI-001: 너무 각져서 딱딱하다 — 적당한 둥글기와 움직임
**상태**: done → `docs/sprints/` (스프린트 종료 시 아카이브)
**우선순위**: medium
**관련 파일**: `apps/web/tailwind.config.ts`, `apps/web/app/globals.css`, `apps/web/components/gov/`

### 설계 구상
현재 `borderRadius.DEFAULT = 2px` 로 정부 문서 톤을 잡았는데 지나치게 경직됐다.
토스처럼 **완전히 둥글게가 아니라** 6~10px 수준으로 올리고, 상호작용에 전이를 준다.
정부 포털의 신뢰감(흰 바탕·네이비·표 중심)은 유지한다 — 색계와 정보 구조는 그대로.

움직임은 **상태 변화를 설명하는 것만** 넣는다. 장식용 애니메이션은 넣지 않는다.
`prefers-reduced-motion` 을 반드시 존중한다.

### 완료 기준
- [ ] 반경 토큰 상향 (카드/패널/버튼/입력), 표는 각지게 유지
- [ ] hover·focus·활성 전이, 값이 바뀌는 카드에 진입 전이
- [ ] `prefers-reduced-motion: reduce` 에서 전이 제거
- [ ] `ui_check` 재실행 — 대비·터치 회귀 없음 (현재 페이지당 1~3건)
- [ ] harness apps/web 통과

### 제약사항
- 대비율은 이미 계산해 맞춰 둔 값이다. 색을 바꾸면 다시 계산할 것
- 리포트(`.sheet`)는 인쇄물이다. 반경·전이를 넣지 않는다

---

### DATA-001: 출하월이 38작목 중 6개뿐
**상태**: in_progress
**우선순위**: high
**관련 파일**: `apps/api/stats/` (신규 수집 모듈), `apps/api/data/crops.json`

### 설계 구상
월별 현금흐름의 핵심 입력인데 6작목만 있다. 나머지는 12개월 균등으로 펼쳐져
"수확기에 몰려 들어온다"는 이 서비스의 주장이 대부분의 작목에서 안 보인다.

KAMIS 일별 도매가에서 **실제 거래가 잡히는 달**을 역산한다. 출하기가 아니면 시세가
안 잡히거나 표본이 급감하므로, 월별 관측일수 분포에서 임계값을 넘는 달을 출하월로 본다.
임계값은 지어내지 않고 이미 출하월을 아는 6작목으로 **검증**한 뒤 고정한다.

KAMIS 매핑이 없는 17작목은 채우지 못한다 — 균등 배분 + '미상' 표시를 유지한다.

### 완료 기준
- [ ] 기존 6작목의 출하월을 이 방법으로 재현 (검증)
- [ ] KAMIS 매핑 21작목에 출하월 반영
- [ ] 매핑 없는 작목은 '미상' 유지 — 추정값으로 채우지 않는다
- [ ] `harness apps/api/stats` 통과, `deps_check` 위반 0

### 제약사항
- **core 는 건드리지 않는다.** 수집은 adapters, `cashflow.py` 는 이미 미상을 처리한다
- 인증키 없이 클론한 사람도 동작해야 한다 — 결과는 `crops.json` 에 커밋
- 임계값 근거를 `crops.json` 또는 결정문서에 남길 것

### Reviewer Findings
(DATA-001 은 신규 수집 모듈이라 복잡 작업 — maker-review-loop 적용)

### Final Gate
- [ ] Reviewer PASS 선언
- [ ] harness all 통과
- [ ] Medium 이상 findings 기록 완료
