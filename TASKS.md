# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 화면 중복 정리 + 「버틸 수 있는 선」 (2026-09-06)

유저 지적 둘.

**① "AI 농사 자금지도랑 수익 전망이랑 다른 게 뭐임?"** — 겹친다. 두 화면이 25년
자금지도와 월별 현금흐름을 똑같이 그리고, 안내 문구도 **둘 다** "월별로 돈이 언제
부족한지"를 자기 역할이라 말한다. 이름이 아니라 **질문이 겹친다.**

**② "AI 써놓을 만한 거 없나"** — 5단계 중 02·03 은 "AI" 를 붙여 놓고 LLM 을 한 줄도
안 쓴다. 특히 03은 유저가 "key 기능" 이라 한 화면인데 계산기처럼 보인다.
숫자를 엔진이 만드는 건 제1원칙이라 바꿀 게 아니고, 문제는 **보여주는 방식**이다.

> 근거: 이 세션의 화면 대조(2026-09-06) · 유저 확인

---

### UX-020: 수익 전망에서 25년 자금지도를 뺀다

**상태**: done
**우선순위**: high
**관련 파일**: `apps/web/app/app/revenue/page.tsx`

동료가 바꾼 이름이 선을 그어 준다 — `농사 수입과 지출` 은 **농사** 관점,
`AI 농사 자금지도` 는 **대출** 관점이다. 그 선을 따른다.

| 자금지도 | 수입과 지출 |
|---|---|
| 빌릴 금액 + 25년 부담 + 그 금액에서 어느 달이 빠듯한가 | 한 해 얼마 벌고 쓰나 + 월별 표 |

**완료 기준**: revenue 에서 자금지도 섹션과 그 fetch 가 사라지고, 자금지도로 가는
링크만 남는다. 두 화면의 lead 문구가 서로 다른 질문을 말한다. harness all 통과.

---

### RISK-010: 「버틸 수 있는 선」 — 고정 시나리오 대신 경계를 찾는다

**상태**: done
**우선순위**: high
**관련 파일**: `apps/api/engine/` (신규), `apps/web/app/app/safety/`

지금 안전진단은 고정 시나리오 5개를 대입한다. 딸기 1,300평 기준 실측:

```
가격 하락    소득 -49.2%  못 버팀
생산량 감소  소득 -73.9%  못 버팀
복합 충격    소득 -108.3% 못 버팀
```

**거의 다 "못 버팀"** 이다. 남의 가정이라 이 농가에 얼마나 가까운 위험인지 알 수 없고,
농가에게는 "그래서 어쩌라고" 가 남는다.

거꾸로 찾는다 — **이 농가는 어디까지 버티나.**

> 도매가가 지금보다 **N%** 떨어지는 데까지는 버팁니다.
> 그 아래로는 6년차에 무너집니다.

**왜 이게 AI 처럼 보이나**: 시나리오를 대입하는 게 아니라 **경계를 탐색**한다.
이분법 탐색은 `levers.solve_for` 에 이미 있고(생활비·면적 축), 몬테카를로도 있다.
**가격 축을 하나 더 여는 일**이다. 숫자는 전부 엔진이 만들고 LLM 은 문장만 쓴다.

**완료 기준**
- 엔진 함수가 가격 충격의 경계값을 돌려준다 (crisis_prob 이 감내 기준을 넘는 지점)
- 경계가 없으면(어떤 하락에도 버팀 / 지금도 못 버팀) 그 사실을 그대로 밝힌다
- 결정론: 같은 입력에 같은 답. 시드 고정 검사 포함
- 화면이 경계값과 "그 아래로는 몇 년차" 를 함께 보여준다

**제약사항**
- `engine/` 이므로 프롬프트·환경변수·네트워크 금지 (폴더 금지사항)
- 외부 시세 API 에 **의존하지 않는다** — apis.data.go.kr 이 지금 504 이고
  캐시 파일도 아직 없다. 시세 연결은 있으면 좋은 한 줄이지 주인공이 아니다.

---

### UX-021: 제공기관 API 가 죽어도 화면이 빈칸으로 남지 않게

**상태**: todo
**우선순위**: high
**관련 파일**: `apps/web/app/market/`, `apps/api/main.py`

**확인한 사실** (2026-09-06, 재현 명령어 아래)
- `apis.data.go.kr` 이 TLS 핸드셰이크에서 멈춘다. 8초·20초·45초 모두 실패 —
  **상한 탓이 아니라 호스트가 죽어 있다.** `api.odcloud.kr` 은 200 으로 잘 온다.
- 그래서 `market/recent` 가 items 0 을 돌려주고, **폴백 캐시가 채워질 기회가 없었다** —
  `data/market_last_known.json` 이 한 번도 생긴 적이 없다(쓰기 조건이 `if items`).
- 저장소에 가격 **원자료가 없다**. crops.json 에는 집계 통계만 있다
  (annual_price_sigma, price_movement_ratio, window, trading_days).
  → **지금 실제 가격을 보여줄 방법이 없다. 지어내지 않는다.**

**그래서 하는 것**: 없는 값을 만들지 않고, **있는 것으로 화면을 채운다.**

화면 전체가 비는 게 아니다. 「가격 변동성」 섹션은 저장된 통계로 이미 잘 돈다
(2022-08-27~2026-08-26 · 396거래일). 비는 곳은 상단 두 곳이다 —
「최근 도매가」와 「연도별 도매가격 요약」.

**완료 기준**
- 그 두 곳이 "자료를 모으고 있어요"(우리가 미완성인 것처럼 읽힌다) 대신
  **왜 못 보여주는지**를 밝힌다 — 제공기관 응답 없음, 우리가 가진 것은 무엇인지.
- 살아 있을 때 캐시가 실제로 채워지는지 확인할 수단을 남긴다(그래야 복구 시 폴백이 산다).
- 심사위원이 이 화면을 열었을 때 **빈 상자를 보지 않는다.**

**하지 않는 것**: 가격을 추정해 채우기. 근거가 없다.

**재현**
```
curl -s -o /dev/null -w "%{http_code} %{time_total}s\n" --max-time 45 https://apis.data.go.kr
curl -s "localhost:8000/api/v1/market/recent?crop_id=strawberry_hydro&limit=5"
ls apps/api/data/market_last_known.json
```

---

### 대기 — 시간이 남으면

시세 한 줄: "지금 시세는 그 경계보다 N% 위에 있습니다."
동료가 만든 시세 화면이 지금은 내 대출과 연결이 없다. 이게 연결 고리가 된다.
**외부 API 가 불안정하므로 없으면 그 줄만 빠지게** 만든다.
