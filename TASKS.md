# TASKS — 현재 스프린트

> 최대 3개만. 나머지는 BACKLOG.md 대기.
> 완료 태스크 **본문**은 여기 두지 않는다 — docs/sprints/ 링크만. (harness Doc Lint 가 강제)

## 현재 스프린트: 자가진단 마무리 (2026-08-28)

유저 지적: 자가진단을 다 채워도 **새로 생기는 게 없다.** 각 줄이 따로 바뀌기만 하고
종합이 없어서 "그래서?" 가 남는다.

여기서 멈춘 이유는 있었다 — 종합하면 "자격 있음/없음" 판정처럼 읽히고, 잘못 판정하면
받을 수 있는 사람이 포기한다. 그런데 **판정하지 않으면서 종합하는 것**은 가능하다:
몇 개 중 몇 개인지 세고, 남은 항목이 무엇인지 짚고, 그 조항으로 보내는 것까지.

> 근거: `docs/sprints/2026-08-28-remaining-feedback.md` · 유저 확인 (의견 수준 유지)

---

### UX-016: 자가진단 종합 — 판정하지 않고 세어 준다

**상태**: done
**우선순위**: high
**관련 파일**: `apps/web/components/EligibilityCheck.tsx`, `apps/web/app/policy/`

### 설계 구상

지금은 6줄이 각각 초록/노랑으로 바뀌기만 한다. 다 누른 사람에게 돌아오는 것이 없다.

**세기만 한다. 판정하지 않는다.**

| 한다 | 하지 않는다 |
|---|---|
| "6개 중 5개는 기준 범위 안이에요" | "자격이 있어요" · "신청 가능해요" |
| 걸린 항목 이름과 조항 번호를 짚는다 | "이 항목 때문에 안 돼요" |
| 안 답한 항목은 **따로 세고 섞지 않는다** | 빈 답을 충족·미충족 어느 쪽으로도 넣기 |
| 남은 조항을 「제도 근거 검색」으로 보낸다 | 우리 판단을 최종처럼 보이게 하기 |

안 답한 것을 세지 않는 것이 핵심이다 — 6개 중 2개만 답했는데 "5개 범위 안" 이라고
하면 거짓이다. `verdictFor` 가 이미 `none` 을 내주므로 그것을 그대로 쓴다.

### 완료 기준

- [x] 상품별로 종합 한 줄 — 답한 것 중 범위 안 / 걸린 것 / 아직 안 답한 것
- [x] 걸린 항목의 라벨과 조항 번호를 나열하고 「제도 근거 검색」 링크
- [x] 하나도 안 답하면 종합을 **표시하지 않는다**
- [x] "자격 있음/없음"·"신청 가능" 류 문구 없음 — 회귀 테스트로 고정
- [x] `ui_check all` 회귀 없음 · harness 통과

### 제약사항

- **안 답한 항목을 어느 쪽으로도 세지 않는다.** 따로 센다
- 종합에도 "최종 판단은 시·군·구" 를 붙인다
- 개수는 화면이 세도 된다 (요건 목록 길이다 — 엔진의 금액·확률이 아니다)

### 구현 세부사항

`tally()` — 답한 것만 센다. `verdictFor` 의 `none` 을 그대로 써서 안 답한 것을
어느 쪽으로도 넣지 않는다. 실측: 3개 답하면 "답하신 3개 중 1개는 기준 범위 안이에요.
2개는 해당하지 않을 수 있어요. 아직 안 답하신 것 2개."

**「원문 펴기」 는 검색으로 보내지 않는다.** 처음엔 `/policy?q=교육실적` 으로 보내려
했는데 RAG 가 엉뚱한 조항(Ⅲ-라)을 냈다. **우리는 이미 정확한 조문을 손에 들고 있다** —
그 자리에서 접힘을 편다.

### 곁들여 잡은 것 — 상단 메뉴가 두 개 동시에 열림

유저 제보. `group-hover` **또는** `group-focus-within` 으로 열고 있어서, 버튼을
클릭해 포커스가 남은 메뉴와 hover 한 메뉴가 겹쳐 떴다. 그 버튼엔 클릭 핸들러조차
없어 눌러도 아무 일이 없었다. 상태 하나로 바꾸고 닫는 길(Escape·바깥 클릭·마우스
이탈·페이지 이동)을 붙였다. 실측: 어떤 순서로 눌러도 **열린 개수 ≤ 1**.

### Reviewer

**PASS** — 종합이 판정으로 새지 않는지, 안 답한 것을 섞지 않는지 테스트로 고정.
`harness all` 통과 · `ui_check all` 0건 · build OK.
