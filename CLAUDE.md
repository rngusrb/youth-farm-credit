# {프로젝트명} — 작업 지침

> 이 파일은 매 세션 자동 로드된다. **짧게 유지한다.**
> 설명·아키텍처는 DEV_GUIDE.md 로, 현재 할 일은 TASKS.md 로 보낸다.
> 여기 있어야 하는 것: ① 문서 지도 ② 절대 원칙 ③ 완료 스프린트 1줄 로그.

## 문서 계층

| 파일 | 역할 |
|------|------|
| **CLAUDE.md** (이 파일) | 핵심 원칙 + 완료 스프린트 목록 |
| **DEV_GUIDE.md** | 아키텍처 지도, 데이터 흐름, "X를 바꾸려면 어디 봐라" 색인 |
| **WORKFLOW.md** | 태스크 lifecycle, 역할, 심각도 기준 |
| **TASKS.md** | 현재 스프린트 (최대 3개, 현재판 전용) |
| **BACKLOG.md** | 발견된 이슈 + 다음 스프린트 대기열 |
| **각폴더/_GUIDE.md** | 폴더별 금지사항 + 핵심 패턴 (설명서 아님) |
| **docs/sprints/** | 완료 스프린트 아카이브 |

**작업 시작 전 반드시**: DEV_GUIDE.md → TASKS.md → 해당 폴더/_GUIDE.md

---

## 핵심 원칙

- **Silent Failure 금지**: `except Exception: pass`, 빈 값 반환, 로그 없는 fallback — 반드시 로깅.
  조용한 성공보다 시끄러운 실패.
- **"완료" 기준**: `python scripts/harness.py all` 통과 + 해당 _GUIDE.md 금지사항 위반 없음.
  - 테스트 러너 단독 실행은 완료 검증으로 **인정하지 않음** (Doc Lint 누락)
  - 단축 명령: `sh scripts/verify.sh`
- **테스트 루프**: 실패 → 원인 파악 → 수정 → 재실행. **같은 실패 2회 연속이면 중단하고 보고.**
- **문서 갱신 범위**: 단순 버그픽스 → _GUIDE.md만. 구조 변경 → DEV_GUIDE.md도.
- **규칙은 사고에서만 생긴다**: 금지사항을 추가할 땐 반드시 **왜 생겼는지(사고 이력)** 를 같이 적는다.
  이유 없는 규칙은 다음 사람이 지운다.
- **감사 체계**: ① 중요한 판단이 걸린 작업은 **컨텍스트 없는 별도 세션의 적대적 리뷰** 대상
  (문서만 주고 "오류를 찾아라") ② 유저는 세션당 1회 **추적 질문**("이 숫자 어디서 나왔나")
  — 재현 명령어 수준까지 소급 답변 의무 ③ 모든 결과 주장에 재현 명령어 첨부.

---

## 두 가지 작업 프로토콜

**단순 작업** (버그픽스, 단일 파일):
```
_GUIDE.md 확인 → harness <폴더>/ → 수정 → harness 재실행 → 통과
```

**복잡한 작업** (신규 모듈, 구조 변경):
```
maker-review-loop 스킬 사용 (Reviewer PASS 없이 sprint-close 금지)
```

---

## 완료된 스프린트

| 날짜 | 스프린트 |
|------|---------|
| YYYY-MM-DD | (sprint-close 가 1줄씩 추가. 상세는 docs/sprints/ 참조) |

> ⚠️ 여기에 상세 이력 bullet 을 쓰지 말 것. 한 줄, 링크는 아카이브에.

---

## CLAUDE.md 업데이트 원칙

| 섹션 | 업데이트 시점 |
|------|-------------|
| 핵심 원칙 | 워크플로우 자체가 바뀔 때만 |
| 완료된 스프린트 | 스프린트 완료 시 1줄 추가 |
