# 청년농 여신 설계 서비스 — 작업 지침

> 이 파일은 매 세션 자동 로드된다. **짧게 유지한다.**
> 설명·아키텍처는 DEV_GUIDE.md 로, 현재 할 일은 TASKS.md 로 보낸다.

청년 농업인이 정책자금을 신청하기 전에 **감당 가능한 차입 규모**를 스스로 계산하고
상환 위험을 미리 확인하는 서비스. 하지 않는 것 — 부도 예측, 신용평가, 대출 알선, 상품 추천.

## 문서 계층

| 파일 | 역할 |
|------|------|
| **CLAUDE.md** (이 파일) | 핵심 원칙 + 완료 스프린트 목록 |
| **DEV_GUIDE.md** | 아키텍처 지도, "X를 바꾸려면 어디 봐라" 색인 |
| **WORKFLOW.md** | 태스크 lifecycle, 역할, 심각도 기준 |
| **TASKS.md** | 현재 스프린트 (최대 3개, 현재판 전용) |
| **BACKLOG.md** | 발견된 이슈 + 다음 스프린트 대기열 |
| **meta/boundaries.yaml** | 레이어 경계 선언 (deps_check 가 집행) |
| **각폴더/_GUIDE.md** | 폴더별 금지사항 + 핵심 패턴 (설명서 아님) |
| **docs/decisions/** | 설계 결정 기록 — 특히 **안 하기로 한 것** |
| **docs/sprints/** | 완료 스프린트 아카이브 |

**작업 시작 전 반드시**: DEV_GUIDE.md → TASKS.md → 해당 폴더/_GUIDE.md

---

## 이 프로젝트의 제1 원칙

> **숫자는 LLM 이 생성하지 않는다.**

모든 금액·확률·비율은 `apps/api/engine/` 의 결정론적 코드가 계산한다.
LLM 은 (a) 자연어 → 구조화 입력, (b) 계산 결과 → 자연어 설명 두 가지만 담당하고,
LLM 문장의 수치는 엔진 출력과 대조해 **불일치 문장을 제거한 뒤** 반환한다.

이 원칙은 문장이 아니라 **배선으로 강제**된다 — `meta/boundaries.yaml` 이
`core: []` 를 선언하고 `scripts/deps_check.py` 가 매 커밋 검사한다.
core(engine·estimators)가 외부 의존 0인 상태가 이 주장의 기계적 증거다.
근거 → `docs/decisions/0001-pure-estimators-belong-to-core.md`

---

## 핵심 원칙

- **Silent Failure 금지**: `except Exception: pass`, 빈 값 반환, 로그 없는 fallback — 반드시 로깅.
- **"완료" 기준**: `python scripts/harness.py all` 통과 + 해당 _GUIDE.md 금지사항 위반 없음.
  - 테스트 러너 단독 실행은 완료 검증으로 **인정하지 않음** (Doc Lint·경계 검사 누락)
  - 단축 명령: `sh scripts/verify.sh`
- **테스트 루프**: 실패 → 원인 → 수정 → 재실행. **같은 실패 2회 연속이면 중단하고 보고.**
- **문서 갱신 범위**: 단순 버그픽스 → _GUIDE.md만. 구조 변경 → DEV_GUIDE.md도.
- **규칙은 사고에서만 생긴다**: 금지사항엔 반드시 **사고 이력**을 같이 적는다.
  다른 프로젝트에서 가져온 규칙은 `**이식된 규칙**` 으로 표시한다 — 겪지도 않은 일을
  사고 이력으로 적으면 가짜 근거가 되어 아무도 못 지운다.
- **API 키 없이도 전체 플로우가 돈다**: 키가 없으면 규칙기반 슬롯 추출 + 템플릿 해설로 전환.
  이 성질을 깨는 변경 금지.
- **감사 체계**: ① 중요한 판단이 걸린 작업은 **컨텍스트 없는 별도 세션의 적대적 리뷰** 대상
  (문서만 주고 "오류를 찾아라") ② 유저는 세션당 1회 **추적 질문**("이 숫자 어디서 나왔나")
  — 재현 명령어 수준까지 소급 답변 의무 ③ 모든 결과 주장에 재현 명령어 첨부.
  > 2026-08-26 이 두 원칙이 CLAUDE.md 재작성 중 누락됐다가 다른 세션의 적대적 리뷰로 적발됐다.
  > **감사 장치가 사라진 것을 감사 대상이 몰랐고 남이 알려줬다** — 이 체계가 필요한 이유 그 자체.

---

## 두 가지 작업 프로토콜

**단순 작업** (버그픽스, 단일 파일):
```
_GUIDE.md 확인 → harness <폴더>/ → 수정 → harness 재실행 → 통과
```

**복잡한 작업** (신규 모듈, 레이어 경계에 닿는 변경):
```
maker-review-loop 스킬 사용 (Reviewer PASS 없이 sprint-close 금지)
```

---

## 완료된 스프린트

| 날짜 | 스프린트 |
|------|---------|
| 2026-08-26 | vibe-kit 골격 이식 + 레이어 경계 확립 — estimators/ 분리로 core 순수성 확보 (`docs/decisions/0001-pure-estimators-belong-to-core.md`) |
| 2026-08-26 | 리포트 한 장 → 정부 포털형 양면 서비스 — RAG 코퍼스 3종·월별 현금흐름·스트레스 테스트 (`docs/sprints/2026-08-26-service-buildout.md`) |
| 2026-08-27 | 팀 피드백 2차 — 기준 시점 표시·근거 접기·출처 구분, 안 돌던 테스트 2개와 안 검사되던 심사 화면 발견 (`docs/sprints/2026-08-27-team-feedback-2.md`) |
| 2026-08-28 | 농가가 읽기 쉽게 — 뜻이 앞·용어는 뒤, 내 조건 필터, 자격은 판정 대신 의견, 금액별 비교 (`docs/sprints/2026-08-28-readable-for-farmers.md`) |
| 2026-08-28 | 남은 피드백과 검사 구멍 — ui_check 가 로그인 뒤 화면도 검사(숨어 있던 위반 24건), 자금 고른 근거, 지난 분석 대비 변화 (`docs/sprints/2026-08-28-remaining-feedback.md`) |

> ⚠️ 여기에 상세 이력 bullet 을 쓰지 말 것. 한 줄, 상세는 아카이브에.

---

## CLAUDE.md 업데이트 원칙

| 섹션 | 업데이트 시점 |
|------|-------------|
| 제1 원칙 / 핵심 원칙 | 워크플로우 자체가 바뀔 때만 |
| 완료된 스프린트 | 스프린트 완료 시 1줄 추가 |
